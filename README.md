## Видео-плеер на основе конечного автомата

Видео-плеер, реализованный с использованием XState для управления состоянием через конечный автомат.

🔗 [Демо на Vercel](http://state-machine-modulbank.vercel.app/)

### 🚀 Технологии

React 19, TypeScript, XState 5, Ant Design, React Player, Vite

### 📁 Структура проекта

```
src/
├── features/
│   └── video-player/      # Модуль видео-плеера
│       ├── index.ts       # Публичный API модуля
│       ├── lib/
│       │   └── useVideoPlayer.ts      # Хук для работы с плеером
│       ├── model/
│       │   ├── videoPlayerMachine.ts  # Конечный автомат
│       │   └── VideoPlayerContext.tsx # React Context
│       └── ui/
│           ├── VideoPlayer.tsx        # Компонент плеера
│           └── OpenPlayerButton.tsx   # Кнопка открытия
├── App.tsx
└── main.tsx
```

### 🛠 Состояния и переходы

**Состояния:**

- `closed` - Модальное окно закрыто
- `opened` - Модальное окно открыто
  - `playing` - Видео воспроизводится (начальное)
  - `paused` - Видео на паузе

**События:**

- `open` - Открыть плеер
- `close` - Закрыть плеер
- `togglePlay` - Переключить воспроизведение/паузу
- `toggleSize` - Изменить размер (`small` / `large`)

**Контекст:**

- `screenSize: 'small' | 'large'` (по умолчанию `'large'`)
